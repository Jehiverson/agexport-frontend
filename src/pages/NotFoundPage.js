import React, { Fragment } from "react";
import Nav from "../components/Nav";

const NotFoundPage = () => {
  return (
    <Fragment>
      <Nav />
    </Fragment>
  );
};

export default NotFoundPage;
